//Strings are immutable, they cannot be altered, so just reassign the variable to a
//new string to change the string.

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me

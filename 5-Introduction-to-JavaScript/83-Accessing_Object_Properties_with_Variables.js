//We can create a variable that is the same as the property within an object,
//then we can use that variable in bracket notation with the object to call the
//value associated with the property.

// Setup
var testObj = {
  12: "Namath",
  16: "Montana",
  19: "Unitas"
};

// Only change code below this line;

var playerNumber = 16;       // Change this Line
var player = testObj[playerNumber];   // Change this Line
